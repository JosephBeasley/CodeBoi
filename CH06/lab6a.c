#include <math.h>
#include <stdio.h>
#include <inttypes.h>

int main()
{
    uint32_t input = 0;
    uint32_t check = 0;
    fprintf(stdout, "Please input a positive number.\n");
    scanf("%u", &input);
    printf("your number was %u \n"), &input);
    return (0);
}